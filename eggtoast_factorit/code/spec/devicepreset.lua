local devicepreset = {
    pc={
        console = false,
        padonly = false,
        noclick = false
    },
    nx={ --possible, but cannot run on lovepotion atm
        console = true,
        padonly = true,
        noclick = true
    }
}
return devicepreset